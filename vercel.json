{
  "crons": [
    {
      "path": "/api/whatsapp/cron-timeout?key=fogao-cron-2024",
      "schedule": "*/3 * * * *"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
